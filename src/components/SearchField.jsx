import React from "react";

  const Styles = {
    border: "1px solid gray"
  }
function SearchField(props) {
  return(
    <input
      type="text"
      defaultValue="Search"/>
  )

}

export default SearchField
